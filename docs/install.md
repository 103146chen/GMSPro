# 🚀 安裝與部署指南

本系統基於 **Google Apps Script (GAS)** 運作。您不需要購買伺服器，只要有 Google 帳號即可免費架設。

## 步驟 1：建立專案副本

### 方法 A：使用範本 (推薦)
1. 點擊此連結開啟 [GMSPro 部署範本試算表](#) *(這裡您之後可以放一個您設定好的 Google Sheet 唯讀連結)*。
2. 點擊左上角 **「檔案」** > **「建立副本」**。
3. 新的檔案將會儲存在您的雲端硬碟中。

### 方法 B：手動建立 (開發者適用)
1. 前往 [Google Drive](https://drive.google.com/) 新增一個 Google Sheets。
2. 點擊 **「擴充功能」** > **「Apps Script」**。
3. 將 GitHub 中的程式碼 (`.js` 和 `.html`) 複製貼上到對應的檔案中。

---

## 步驟 2：初次設定

1. 開啟 Apps Script 編輯器。
2. 找到 `_setup.js` (或初始化函式)，點擊上方工具列的 **「執行」**。
3. **授權權限**：
   - 系統會跳出「需要授權」視窗，請點選「審查權限」。
   - 選擇您的 Google 帳號。
   - ⚠️ **看到「Google 尚未驗證這個應用程式」怎麼辦？**
     - 這是正常現象（因為這是您自己跑的程式）。
     - 請點選左下角的 **「進階 (Advanced)」**。
     - 再點選最下方的 **「前往... (不安全)」**。
     -最後點選 **「允許」**。

---

## 步驟 3：部署為 Web App

這是最關鍵的一步，設定錯誤將導致學生無法查詢。

1. 點擊編輯器右上角的 **「部署 (Deploy)」** > **「新增部署作業 (New deployment)」**。
2. 點擊左側齒輪圖示，選擇 **「網頁應用程式 (Web app)」**。
3. 設定如下：
   - **說明**：輸入版本號 (例如 v10.0)。
   - **執行身分 (Execute as)**：選 **「我 (Me)」** (重要！這樣程式才有權限讀取您的試算表)。
   - **誰可以存取 (Who has access)**：選 **「任何人 (Anyone)」** (包含匿名使用者)。
     - *注意：若您是 Google Workspace 教育帳號，可能需要聯絡管理員開啟「允許任何人」的權限，否則只能選「網域內的使用者」。*
4. 點擊 **「部署」**。
5. 複製生成的 **網頁應用程式網址 (Web App URL)**。

🎉 **恭喜！這串網址就是您的 GMSPro 入口網站，可以發給學生和家長了！**

---

## 常見問題 (FAQ)

**Q: 更新程式碼後，學生看到的介面沒變？**
A: GAS 的 Web App 每次修改程式碼後，都必須 **「建立新版本」** 的部署。
請去 **部署** > **管理部署作業** > 點擊筆 (編輯) > **版本** 選擇「建立新版本」 > 儲存。